package conector;

import back.Usuario;

public class FrontToBack {
/*
    public static void main(String[] args) {

        String datosArchivo = LecturaEscrituraFicheros.leerArchivo();  // ver error con archivo alvaro


        Conector conector = new Conector(new UsuarioDAO()); // falta implementar en back
        boolean resultado = conector.crearUsuarioDesdeJson(datosArchivo);

        if (resultado) {
            System.out.println("Los datos del archivo se enviaron correctamente al backend.");
        } else {
            System.out.println("Error al enviar los datos del archivo al backend.");
        }
    }*/
}
